# ===============================================================
# Título: Template de Variáveis de Ambiente - Espaço Vital
# Descrição: Arquivo modelo para configuração de ambientes
# Autor: Will
# Data: 05/10/2025
# ===============================================================

# AMBIENTE
# Valores possíveis: development, qa, production
ENVIRONMENT=development

# DJANGO
SECRET_KEY=sua-secret-key-super-segura-aqui
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# DATABASE
DB_ENGINE=django.db.backends.postgresql
DB_NAME=espacovital_dev
DB_USER=postgres
DB_PASSWORD=sua-senha-aqui
DB_HOST=localhost
DB_PORT=5432

# EMAIL (Configurar depois)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# STORAGE (S3 ou local)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1

# SEGURANÇA
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# RAILWAY (apenas para QA/Production)
RAILWAY_ENVIRONMENT=